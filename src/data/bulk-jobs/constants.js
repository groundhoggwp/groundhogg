import { NAMESPACE } from '../constants';

/**
 * Internal dependencies
 *
 * @todo: Note: this API endpoint does not currently exist.
 * @todo: This is the name of the store, not _necessarily_ identical to the API endpoint.
 */
export const STORE_NAME = `${NAMESPACE}/bulk-jobs`;