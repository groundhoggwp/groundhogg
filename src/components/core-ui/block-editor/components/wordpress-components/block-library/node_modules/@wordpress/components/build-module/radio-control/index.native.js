import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import { createElement, Fragment } from "@wordpress/element";

/**
 * Internal dependencies
 */
import RadioCell from '../mobile/bottom-sheet/radio-cell';

function RadioControl(_ref) {
  var onChange = _ref.onChange,
      selected = _ref.selected,
      _ref$options = _ref.options,
      options = _ref$options === void 0 ? [] : _ref$options,
      props = _objectWithoutProperties(_ref, ["onChange", "selected", "options"]);

  return createElement(Fragment, null, options.map(function (option, index) {
    return createElement(RadioCell, _extends({
      label: option.label,
      onPress: function onPress() {
        return onChange(option.value);
      },
      selected: option.value === selected,
      key: "".concat(option.value, "-").concat(index)
    }, props));
  }));
}

export default RadioControl;
//# sourceMappingURL=index.native.js.map