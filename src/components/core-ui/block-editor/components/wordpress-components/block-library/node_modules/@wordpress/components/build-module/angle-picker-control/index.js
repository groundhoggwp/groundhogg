import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import { createElement } from "@wordpress/element";

/**
 * External dependencies
 */
import classnames from 'classnames';
/**
 * WordPress dependencies
 */

import { useInstanceId } from '@wordpress/compose';
/**
 * Internal dependencies
 */

import BaseControl from '../base-control';
import { FlexBlock } from '../flex';
import NumberControl from '../number-control';
import AngleCircle from './angle-circle';
import { Root, NumberControlWrapper } from './styles/angle-picker-control-styles';
export default function AnglePickerControl(_ref) {
  var className = _ref.className,
      idProp = _ref.id,
      value = _ref.value,
      onChange = _ref.onChange,
      label = _ref.label,
      props = _objectWithoutProperties(_ref, ["className", "id", "value", "onChange", "label"]);

  var instanceId = useInstanceId(AnglePickerControl, 'components-angle-picker-control__input');
  var id = idProp || instanceId;

  var handleOnNumberChange = function handleOnNumberChange(unprocessedValue) {
    var inputValue = unprocessedValue !== '' ? parseInt(unprocessedValue, 10) : 0;
    onChange(inputValue);
  };

  var classes = classnames('components-angle-picker-control', className);
  return createElement(BaseControl, _extends({
    className: classes,
    id: id,
    label: label
  }, props), createElement(Root, {
    gap: 3
  }, createElement(NumberControlWrapper, null, createElement(NumberControl, {
    className: "components-angle-picker-control__input-field",
    id: id,
    max: 360,
    min: 0,
    onChange: handleOnNumberChange,
    step: "1",
    value: value
  })), createElement(FlexBlock, null, createElement(AngleCircle, {
    "aria-hidden": "true",
    value: value,
    onChange: onChange
  }))));
}
//# sourceMappingURL=index.js.map