import _extends from "@babel/runtime/helpers/esm/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/esm/objectWithoutProperties";
import { createElement } from "@wordpress/element";

/**
 * WordPress dependencies
 */
import { link, linkOff } from '@wordpress/icons';
import { __ } from '@wordpress/i18n';
/**
 * Internal dependencies
 */

import Button from '../button';
import Tooltip from '../tooltip';
export default function LinkedButton(_ref) {
  var isLinked = _ref.isLinked,
      props = _objectWithoutProperties(_ref, ["isLinked"]);

  var linkedTooltipText = isLinked ? __('Unlink Sides') : __('Link Sides');
  return createElement(Tooltip, {
    text: linkedTooltipText
  }, createElement("span", null, createElement(Button, _extends({}, props, {
    className: "component-box-control__linked-button",
    isPrimary: isLinked,
    isSecondary: !isLinked,
    isSmall: true,
    icon: isLinked ? link : linkOff,
    iconSize: 16
  }))));
}
//# sourceMappingURL=linked-button.js.map