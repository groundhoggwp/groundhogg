"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.removeNonDigit = removeNonDigit;
exports.toFixed = toFixed;

function removeNonDigit(text, decimalNum) {
  var regex = decimalNum > 0 ? /^(\d+\.?\,?(\d+)?)/ : /([\d]+)/;
  var result = text.match(regex);
  return result ? result[0] : '';
}

function toFixed(num) {
  var decimalNum = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  var fixed = Math.pow(10, decimalNum < 0 ? 0 : decimalNum);
  return Math.floor(num * fixed) / fixed;
}
//# sourceMappingURL=index.native.js.map