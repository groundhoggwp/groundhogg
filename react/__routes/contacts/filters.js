import React from 'react'

const filters = [
  {
    type: 'first_name',
    name: 'First Name',
    category: 'contact',
    render: ({updateFilter}) => {
      // return (
      //
      // );
    },
    query: () => {

    }
  }
]

const SearchFilters = ({query, filters, addFilter, deleteFilter}) => {

  return (
    <div className={'advanced-search-filters'}>

    </div>
  )
}

const SearchFilter = ({}) => {

}